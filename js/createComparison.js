/**
 * @function
 */
function createComparison(id1, id2) {
    //TODO
    createDetails(id1);
    createDetails(id2);

    /* 
    Først må bruker fylle inn begge søkefeltene med gyldige kommunenr. 
    Her må vi implementere en sjekk for at begge er gyldige. Kanskje også at det 
    ikke kan skrives inn samme nr. i begge feltene.

    createDetails() kan brukes til å lage 2 ulike tabeller, en for hver kommune. 
    Tabellene trenger kun å inneholde historisk data for utvikling av sysselsetting ette kjønn, dermed 
    bør dette i createDetails skilles ut i en egen funksjon.
    Tabellene må være i to ulike divs, slik at Ingeleiv kan flytte de rundt med CSS.

    
    
    */
}